---
interface:
  display_name: "Release Extension"
  short_description: "Prepare and publish VS Code extension releases"
  default_prompt: >-
    Use $release-extension to prepare a VS Code extension release, update
    CHANGELOG, bump version with pnpm, publish with vsce, and open the PR. This
    adapter is outside the seven-role SDD lifecycle.
    Fixed execution metadata: model Luna; reasoning effort medium.
